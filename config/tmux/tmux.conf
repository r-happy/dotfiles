# --- 基本設定 (Nix側で設定済みなら不要ですが、残しておいてもOK) ---
set-option -g status-position bottom
set -ag terminal-overrides ",*:RGB"

# --- 操作設定 ---
# ペイン移動
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# クリップボード連携
set -s set-clipboard on
bind-key -T copy-mode-vi v send -X begin-selection
# WSL の clip.exe は日本語を含む tmux のコピーで文字化けしやすいので、
# UTF-16LE に変換してから Windows クリップボードへ渡す。
# bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "iconv -f UTF-8 -t UTF-16LE | clip.exe"
# bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "iconv -f UTF-8 -t UTF-16LE | clip.exe"
# bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "iconv -f UTF-8 -t UTF-16LE | clip.exe"

# tawny dark theme for tmux
# https://github.com/your-name/tawny.nvim

# status bar base
set -g status-style                  "bg=#211a17,fg=#aa947d"
set -g status-left-length            50
set -g status-right-length           80

# powerline separators (requires Nerd Font / Powerline font)
# solid:
# thin:

set -g window-status-separator ""

set -g status-left "#[bg=#aa947d,fg=#28211d,bold] #S #[fg=#aa947d,bg=#211a17]"

set -g window-status-format "#[bg=#211a17,fg=#aa947d] #{b:pane_current_path}  #I #W#F "

set -g window-status-current-format "#[fg=#211a17,bg=#aa947d]#[bg=#aa947d,fg=#28211d,bold] #{b:pane_current_path} #[fg=#aa947d,bg=#312c27]#[bg=#312c27,fg=#e2d1b3,bold] #I #W#F #[fg=#312c27,bg=#211a17]"

set -g window-status-activity-style  "bg=#211a17,fg=#cdb07a"
set -g window-status-bell-style      "bg=#211a17,fg=#b67a70"

# pane borders
set -g pane-border-style             "fg=#584a40"
set -g pane-active-border-style      "fg=#aa947d"

# message / command prompt
set -g message-style                 "bg=#312c27,fg=#e2d1b3"
set -g message-command-style         "bg=#312c27,fg=#aa947d"

# copy mode
set -g mode-style                    "bg=#312c27,fg=#e2d1b3"

# ポップアップ設定
bind -n M-t if-shell -F '#{==:#{session_name},popup}' \
  'detach-client' \
  'display-popup -d "#{pane_current_path}" -w 90% -h 90% -E "tmux new-session -A -s popup"'
