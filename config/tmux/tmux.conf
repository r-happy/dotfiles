# --- 基本設定 (Nix側で設定済みなら不要ですが、残しておいてもOK) ---
set-option -g status-position bottom
set -ag terminal-overrides ",*:RGB"

# --- 操作設定 ---
# ペイン移動
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# クリップボード連携
set -s set-clipboard on
bind-key -T copy-mode-vi v send -X begin-selection
# WSL の clip.exe は日本語を含む tmux のコピーで文字化けしやすいので、
# UTF-16LE に変換してから Windows クリップボードへ渡す。
# bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "iconv -f UTF-8 -t UTF-16LE | clip.exe"
# bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "iconv -f UTF-8 -t UTF-16LE | clip.exe"
# bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "iconv -f UTF-8 -t UTF-16LE | clip.exe"

# tawny dark theme for tmux
# https://github.com/your-name/tawny.nvim

# status bar base
set -g status-style                  "bg=#100c0a,fg=#927963"
set -g status-left-length            50
set -g status-right-length           80

# powerline separators (requires Nerd Font / Powerline font)
# solid:
# thin:

# status-left: [session name]
set -g status-left \
  "#[bg=#41342c,fg=#f1d8b8,bold] #S #[bg=#100c0a,fg=#927963,nobold] "

# status-right: [host] [date time]
set -g status-right \
  "#[bg=#41342c,fg=#100c0a]#[bg=#41342c,fg=#927963] #h #[bg=#927963,fg=#41342c]#[bg=#927963,fg=#16110e] %Y-%m-%d  %H:%M "

# window list
set -g window-status-format \
  "#[bg=#100c0a,fg=#927963] #{b:pane_current_path}  #I #W#F "

set -g window-status-current-format \
  "#[bg=#927963,fg=#100c0a]#[bg=#927963,fg=#16110e,bold] #{b:pane_current_path} #[bg=#211b17,fg=#927963]#[bg=#211b17,fg=#f1d8b8,bold] #I #W#F #[bg=#100c0a,fg=#211b17]"

set -g window-status-activity-style  "bg=#100c0a,fg=#f4c96b"
set -g window-status-bell-style      "bg=#100c0a,fg=#e26d7d"

# pane borders
set -g pane-border-style             "fg=#41342c"
set -g pane-active-border-style      "fg=#927963"

# message / command prompt
set -g message-style                 "bg=#211b17,fg=#f1d8b8"
set -g message-command-style         "bg=#211b17,fg=#927963"

# copy mode
set -g mode-style                    "bg=#211b17,fg=#f1d8b8"

# ポップアップ設定
bind -n M-t if-shell -F '#{==:#{session_name},popup}' \
  'detach-client' \
  'display-popup -d "#{pane_current_path}" -w 90% -h 90% -E "tmux new-session -A -s popup"'

# bottom popup toggle
bind b display-popup -d "#{pane_current_path}" -x 0 -y 65% -w 100% -h 35% -E "tmux new-session -A -s bottom"
