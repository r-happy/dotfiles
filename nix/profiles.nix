{
  # General-purpose programming tools, including Codex.
  development = true;

  # Keep the full existing TeX environment intact when it is needed, but do
  # not make every normal switch realise TeX Live and OCR dependencies.
  docs = false;

  # Security/CTF tooling is intentionally opt-in.
  ctf = false;
}
